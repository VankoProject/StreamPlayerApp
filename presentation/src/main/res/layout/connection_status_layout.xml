<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/connectionCardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="@dimen/corner_radius_standard"
    app:cardElevation="@dimen/elevation_standard"
    android:layout_marginStart="@dimen/xx_medium_margin"
    android:layout_marginEnd="@dimen/xx_medium_margin"
    android:backgroundTint="@color/darkGrey">

    <LinearLayout
        android:id="@+id/rootConnectionView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/connectionIconImageView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/wifi_off_ic"
            android:layout_gravity="center"/>

        <TextView
            android:id="@+id/connectionErrorTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/no_internet_connection"
            android:textColor="@android:color/white"
            android:padding="16dp"
            android:textSize="@dimen/x_medium_text"
            android:layout_gravity="center" />
    </LinearLayout>

</androidx.cardview.widget.CardView>